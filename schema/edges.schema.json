{
  "$schema": "http://json-schema.org/draft/2020-12/schema",
  "title": "Edges (minimal)",
  "type": "object",
  "description": "Map of edge_id -> {from, to, ...anything}",
  "patternProperties": {
    ".*": {
      "type": "object",
      "properties": {
        "from": { "type": "string" },
        "to":   { "type": "string" }
      },
      "required": ["from", "to"],
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
